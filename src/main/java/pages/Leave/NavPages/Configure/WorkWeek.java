package pages.Leave.NavPages.Configure;

import org.openqa.selenium.WebDriver;
import pages.BaseComponents.BasePage;

public class WorkWeek extends BasePage {

    public WorkWeek(WebDriver driver) {
        super(driver);
    }
}

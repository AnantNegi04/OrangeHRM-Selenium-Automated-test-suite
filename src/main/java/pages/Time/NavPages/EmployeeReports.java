package pages.Time.NavPages;

import org.openqa.selenium.WebDriver;
import pages.BaseComponents.BasePage;

public class EmployeeReports extends BasePage {

    public EmployeeReports(WebDriver driver) {
        super(driver);
    }
}
